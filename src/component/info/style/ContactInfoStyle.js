import styled from 'styled-components';

const Wrapper = styled.div`
  width: 100%;
  display: flex;
  flex-flow: row wrap;
`;

const InfoWrapper = styled.section`
  display: flex;
  flex-flow: column nowrap;
`;

const Picture = styled.section``;

export { Wrapper, InfoWrapper, Picture}